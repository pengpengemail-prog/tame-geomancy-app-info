# TAMEGeomancy 算法说明文档

更新时间：2026-05-03

## 文档目的

本文档用于说明 `TAMEGeomancy` 当前版本的主要堪舆算法、映射规则与展示逻辑，便于后续维护、测试与审核说明。

## 1. 纳气盘算法

### 1.1 固定偏移规则

- 地盘角度：设备当前测得的基础坐向角度
- 纳气盘角度：`地盘角度 + 7.5°`
- 若结果大于等于 `360°`，则执行取模回绕

公式：

```text
naqiAngle = (rawAngle + 7.5) mod 360
```

### 1.2 方位映射

- 二十四山以每 `15°` 为一格
- 通过 `Direction.from(angle:)` 将角度映射到 `壬、子、癸、丑...亥`
- 朝向与坐向取对宫关系

### 1.3 纳气分析逻辑

- 根据纳气口类型、大门权重、当前元运与方位五行关系生成评分
- 输出：
  - 纳气角度
  - 所属方位
  - 五行属性
  - 旺衰等级
  - 参考建议

## 2. 三元九运算法

当前版本使用固定区间判断：

- 七运：`1984–2003`
- 八运：`2004–2023`
- 九运：`2024–2043`

实现入口：

- `SanyuanJiuyun.from(year:)`
- `SanyuanJiuyun.current`

输出内容包括：

- 运期名称
- 当运星
- 五行属性
- 对应时间段

## 3. 玄空飞星算法

### 3.1 运盘

- 当运星入中宫
- 按洛书顺飞

### 3.2 山盘

- 由坐山方位换算中心星
- 阳山顺飞，阴山逆飞

### 3.3 向盘

- 由朝向或纳气口方位换算中心星
- 与山盘飞布方向相反

### 3.4 流年盘

- 使用流年星入中
- 当前实现采用：

```text
liunianStar = 11 - (year % 100 % 9)
```

再做 `1...9` 范围回绕。

### 3.5 九宫综合信息

每宫输出：

- 运星
- 山星
- 向星
- 流年星
- 运星状态
- 山星状态
- 向星状态
- 综合评分
- 吉凶等级

## 4. 旺衰判断规则

### 4.1 状态划分

- `旺`：当前运星
- `生`：下一运星
- `退`：上一运星
- `衰`：其余普通状态
- `煞`：五黄、二黑等重点注意星

### 4.2 当前实现逻辑

- `star == currentStar` -> `旺`
- `star == currentStar + 1` 或 `9 -> 1` 回绕 -> `生`
- `star == currentStar - 1` 或 `1 <- 9` 回绕 -> `退`
- `star == 5 || star == 2` -> `煞`
- 其他 -> `衰`

### 4.3 评分映射

- `旺` -> `100`
- `生` -> `80`
- `退` -> `50`
- `衰` -> `30`
- `煞` -> `10`

宫位总分按以下权重计算：

- 运盘：`40%`
- 山盘：`30%`
- 向盘：`30%`

## 5. 户型图分析算法

### 5.1 输入

- 户型图图片
- 手动立极点
- 手动朝向
- 房间标记

### 5.2 分析过程

1. 根据朝向推算坐山
2. 根据当前年份自动取当前元运
3. 调用 `FlyingStarCalculator.calculateCompletePan(...)`
4. 获取九宫全部宫位信息
5. 依据综合分划分吉位与注意位

### 5.3 方位分类

- `score >= 70` -> 吉位
- `score < 40` -> 注意位
- 中间区间 -> 中性参考

### 5.4 热力图等级

- `80...100` -> `旺位`
- `60..<80` -> `吉位`
- `40..<60` -> `平位`
- `<40` -> `注意位`

### 5.5 保存字段

保存记录时写入：

- 立极点坐标
- 朝向
- 吉位
- 注意位
- 房间标记摘要
- 综合建议

## 6. 八宅算法

### 6.1 输入

- 房屋坐向
- 可选的居住者出生年份

### 6.2 输出

- 宅卦
- 东四宅 / 西四宅
- 八方位位置分布
- 命卦匹配结果
- 空间布置建议

### 6.3 当前定位

当前八宅模块用于空间参考和方位建议，不用于命理承诺或强结论输出。

## 7. 报告导出逻辑

### 7.1 导出内容

- 分类
- 标题
- 摘要
- 时间
- 分析详情
- 用户备注
- 免责声明

### 7.2 导出形式

- 文本摘要
- TAME·Geomancy 风格报告卡图片
- 系统分享面板

### 7.3 合规说明

报告卡底部固定保留：

- “本 APP 内容仅为民俗文化参考”
- “非科学依据”
- “不构成任何现实承诺”

## 8. 当前边界

- 户型图分析尚未引入自动识别房间轮廓或 AI 分割
- 测向与纳气判断依赖设备传感器精度与用户操作
- 当前版本强调民俗文化参考，不输出保证性结论
